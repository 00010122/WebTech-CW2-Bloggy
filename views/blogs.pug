extends layout.pug

block content
    .blog-container
        h1 All Blogs

        if deleted
            .deleted Blog is deleted 

        each blog in blogs 
            .blog-wrapper
                a.blog(href=`/${ blog.id }`) #{ blog.title }   
                a.delete(href=`/${ blog.id }/delete`) delete